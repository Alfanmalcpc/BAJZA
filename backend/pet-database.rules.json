{
  "rules": {
    "aquarium": {
      ".read": true,
      "$uid": {
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['tokens', 'updatedAt'])"
      }
    },
    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
